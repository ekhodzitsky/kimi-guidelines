# Example pre-commit configuration for kimi-guidelines
# Save as .pre-commit-config.yaml in your project root

repos:
  - repo: local
    hooks:
      - id: kimi-guidelines-check
        name: kimi-guidelines check
        entry: cargo kimi check --strictness standard
        language: system
        pass_filenames: false
        always_run: true
