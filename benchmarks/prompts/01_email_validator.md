# Prompt: Email Validator

Write a Rust library that validates email addresses according to RFC 5322.

Requirements:
- Parse and validate email strings
- Return detailed errors (e.g., missing @, invalid domain, forbidden characters)
- Support internationalized email addresses (UTF-8)
- Provide a builder for constructing validated emails
- Unit tests with edge cases
