[package]
name = "rust-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
proptest = "1.0"

[dev-dependencies]
proptest = "1.0"

[lints.clippy]
all = "deny"
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
